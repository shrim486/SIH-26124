# Citizen portal

React/Vite application. Follow [repository setup](../README.md) first.

From this directory: `npm ci`, `npm run dev`, `npm run build`, and `npm run lint`.

Copy `.env.example` to `.env` to customize the API URL; `VITE_` variables are public. The government API requires login using credentials configured in the backend.
